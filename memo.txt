1/24 
2:00:15
今のところ順調です。
特にわからないところはない。

1/26
3:01:40
user progress
neon,drizzleを使えるようになる。
# where は条件一致
# with はリレーションを含めるかどうか

"""
import { drizzle } from 'drizzle-orm/neon-serverless';
import * as schema from "./schema"
const db = drizzle(process.env.DATABASE_URL! , {schema}); 
export default db;
"""